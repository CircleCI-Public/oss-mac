version: 2.1

workflows:
  ci:
    jobs:
      - test

jobs:
  test:
    macos:
      xcode: "12.2"
    resource_class: metal
    steps:
      - checkout
      - run: echo "content" >> file.txt
      - save_cache:
          key: "the-key"
          paths:
            - ./file.txt