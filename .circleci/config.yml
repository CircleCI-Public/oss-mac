version: 2.1

workflows:
  ci:
    jobs:
      - test

jobs:
  test:
    machine:
      resource_class: medium
    steps:
      - run: echo "This is from kcciperm" > file.txt
      - save_cache:
          key: c7981447-68b6-46dd-95f6-8ca4f825e224
          paths:
          - file.txt
